file_name = "tmp.txt"
mx=""
mn=""
with open(file_name) as f:
    for  line in f:
        if(len(line)<lmn) :
            lmn = len(line)
            mn = line
        else :
            if(len(line) > len(mx)) :
                len(mx) = len(line)
                mx = line
print(f"tu dai nhat: {mx} do dai {lmx}")
print(f"tu ngan nhat: {mn} do dai {lmn}"
      @aa =